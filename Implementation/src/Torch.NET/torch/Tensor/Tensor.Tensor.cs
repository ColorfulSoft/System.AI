//***************************************************************************************************
//* (C) ColorfulSoft corp., 2019. All rights reserved.
//* This code is distributed under the Apache v2 license.
//*
//*                                 Apache License
//*                           Version 2.0, January 2004
//*                        http://www.apache.org/licenses/
//*
//*   TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION
//*
//*   1. Definitions.
//*
//*      "License" shall mean the terms and conditions for use, reproduction,
//*      and distribution as defined by Sections 1 through 9 of this document.
//*
//*      "Licensor" shall mean the copyright owner or entity authorized by
//*      the copyright owner that is granting the License.
//*
//*      "Legal Entity" shall mean the union of the acting entity and all
//*      other entities that control, are controlled by, or are under common
//*      control with that entity. For the purposes of this definition,
//*      "control" means (i) the power, direct or indirect, to cause the
//*      direction or management of such entity, whether by contract or
//*      otherwise, or (ii) ownership of fifty percent (50%) or more of the
//*      outstanding shares, or (iii) beneficial ownership of such entity.
//*
//*      "You" (or "Your") shall mean an individual or Legal Entity
//*      exercising permissions granted by this License.
//*
//*      "Source" form shall mean the preferred form for making modifications,
//*      including but not limited to software source code, documentation
//*      source, and configuration files.
//*
//*      "Object" form shall mean any form resulting from mechanical
//*      transformation or translation of a Source form, including but
//*      not limited to compiled object code, generated documentation,
//*      and conversions to other media types.
//*
//*      "Work" shall mean the work of authorship, whether in Source or
//*      Object form, made available under the License, as indicated by a
//*      copyright notice that is included in or attached to the work
//*      (an example is provided in the Appendix below).
//*
//*      "Derivative Works" shall mean any work, whether in Source or Object
//*      form, that is based on (or derived from) the Work and for which the
//*      editorial revisions, annotations, elaborations, or other modifications
//*      represent, as a whole, an original work of authorship. For the purposes
//*      of this License, Derivative Works shall not include works that remain
//*      separable from, or merely link (or bind by name) to the interfaces of,
//*      the Work and Derivative Works thereof.
//*
//*      "Contribution" shall mean any work of authorship, including
//*      the original version of the Work and any modifications or additions
//*      to that Work or Derivative Works thereof, that is intentionally
//*      submitted to Licensor for inclusion in the Work by the copyright owner
//*      or by an individual or Legal Entity authorized to submit on behalf of
//*      the copyright owner. For the purposes of this definition, "submitted"
//*      means any form of electronic, verbal, or written communication sent
//*      to the Licensor or its representatives, including but not limited to
//*      communication on electronic mailing lists, source code control systems,
//*      and issue tracking systems that are managed by, or on behalf of, the
//*      Licensor for the purpose of discussing and improving the Work, but
//*      excluding communication that is conspicuously marked or otherwise
//*      designated in writing by the copyright owner as "Not a Contribution."
//*
//*      "Contributor" shall mean Licensor and any individual or Legal Entity
//*      on behalf of whom a Contribution has been received by Licensor and
//*      subsequently incorporated within the Work.
//*
//*   2. Grant of Copyright License. Subject to the terms and conditions of
//*      this License, each Contributor hereby grants to You a perpetual,
//*      worldwide, non-exclusive, no-charge, royalty-free, irrevocable
//*      copyright license to reproduce, prepare Derivative Works of,
//*      publicly display, publicly perform, sublicense, and distribute the
//*      Work and such Derivative Works in Source or Object form.
//*
//*   3. Grant of Patent License. Subject to the terms and conditions of
//*      this License, each Contributor hereby grants to You a perpetual,
//*      worldwide, non-exclusive, no-charge, royalty-free, irrevocable
//*      (except as stated in this section) patent license to make, have made,
//*      use, offer to sell, sell, import, and otherwise transfer the Work,
//*      where such license applies only to those patent claims licensable
//*      by such Contributor that are necessarily infringed by their
//*      Contribution(s) alone or by combination of their Contribution(s)
//*      with the Work to which such Contribution(s) was submitted. If You
//*      institute patent litigation against any entity (including a
//*      cross-claim or counterclaim in a lawsuit) alleging that the Work
//*      or a Contribution incorporated within the Work constitutes direct
//*      or contributory patent infringement, then any patent licenses
//*      granted to You under this License for that Work shall terminate
//*      as of the date such litigation is filed.
//*
//*   4. Redistribution. You may reproduce and distribute copies of the
//*      Work or Derivative Works thereof in any medium, with or without
//*      modifications, and in Source or Object form, provided that You
//*      meet the following conditions:
//*
//*      (a) You must give any other recipients of the Work or
//*          Derivative Works a copy of this License; and
//*
//*      (b) You must cause any modified files to carry prominent notices
//*          stating that You changed the files; and
//*
//*      (c) You must retain, in the Source form of any Derivative Works
//*          that You distribute, all copyright, patent, trademark, and
//*          attribution notices from the Source form of the Work,
//*          excluding those notices that do not pertain to any part of
//*          the Derivative Works; and
//*
//*      (d) If the Work includes a "NOTICE" text file as part of its
//*          distribution, then any Derivative Works that You distribute must
//*          include a readable copy of the attribution notices contained
//*          within such NOTICE file, excluding those notices that do not
//*          pertain to any part of the Derivative Works, in at least one
//*          of the following places: within a NOTICE text file distributed
//*          as part of the Derivative Works; within the Source form or
//*          documentation, if provided along with the Derivative Works; or,
//*          within a display generated by the Derivative Works, if and
//*          wherever such third-party notices normally appear. The contents
//*          of the NOTICE file are for informational purposes only and
//*          do not modify the License. You may add Your own attribution
//*          notices within Derivative Works that You distribute, alongside
//*          or as an addendum to the NOTICE text from the Work, provided
//*          that such additional attribution notices cannot be construed
//*          as modifying the License.
//*
//*      You may add Your own copyright statement to Your modifications and
//*      may provide additional or different license terms and conditions
//*      for use, reproduction, or distribution of Your modifications, or
//*      for any such Derivative Works as a whole, provided Your use,
//*      reproduction, and distribution of the Work otherwise complies with
//*      the conditions stated in this License.
//*
//*   5. Submission of Contributions. Unless You explicitly state otherwise,
//*      any Contribution intentionally submitted for inclusion in the Work
//*      by You to the Licensor shall be under the terms and conditions of
//*      this License, without any additional terms or conditions.
//*      Notwithstanding the above, nothing herein shall supersede or modify
//*      the terms of any separate license agreement you may have executed
//*      with Licensor regarding such Contributions.
//*
//*   6. Trademarks. This License does not grant permission to use the trade
//*      names, trademarks, service marks, or product names of the Licensor,
//*      except as required for reasonable and customary use in describing the
//*      origin of the Work and reproducing the content of the NOTICE file.
//*
//*   7. Disclaimer of Warranty. Unless required by applicable law or
//*      agreed to in writing, Licensor provides the Work (and each
//*      Contributor provides its Contributions) on an "AS IS" BASIS,
//*      WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
//*      implied, including, without limitation, any warranties or conditions
//*      of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A
//*      PARTICULAR PURPOSE. You are solely responsible for determining the
//*      appropriateness of using or redistributing the Work and assume any
//*      risks associated with Your exercise of permissions under this License.
//*
//*   8. Limitation of Liability. In no event and under no legal theory,
//*      whether in tort (including negligence), contract, or otherwise,
//*      unless required by applicable law (such as deliberate and grossly
//*      negligent acts) or agreed to in writing, shall any Contributor be
//*      liable to You for damages, including any direct, indirect, special,
//*      incidental, or consequential damages of any character arising as a
//*      result of this License or out of the use or inability to use the
//*      Work (including but not limited to damages for loss of goodwill,
//*      work stoppage, computer failure or malfunction, or any and all
//*      other commercial damages or losses), even if such Contributor
//*      has been advised of the possibility of such damages.
//*
//*   9. Accepting Warranty or Additional Liability. While redistributing
//*      the Work or Derivative Works thereof, You may choose to offer,
//*      and charge a fee for, acceptance of support, warranty, indemnity,
//*      or other liability obligations and/or rights consistent with this
//*      License. However, in accepting such obligations, You may act only
//*      on Your own behalf and on Your sole responsibility, not on behalf
//*      of any other Contributor, and only if You agree to indemnify,
//*      defend, and hold each Contributor harmless for any liability
//*      incurred by, or claims asserted against, such Contributor by reason
//*      of your accepting any such warranty or additional liability.
//*
//*    END OF TERMS AND CONDITIONS
//***************************************************************************************************

//-> Latest commit: Brykin Gleb, 06.11.2019.

using System;

public static partial class torch
{

    public sealed partial class Tensor
    {

        //**************************************************
        //-> Half array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">System.Half 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(Half[] data, torch.dtype dtype = torch.dtype.float16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            torch.__Warnings.warn("Hardware support for type float16 is not available in the .NET Framework, so a software implementation is used. You should use float32 or float64 to speed up your application.");
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert Half to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">System.Half 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(Half[,] data, torch.dtype dtype = torch.dtype.float16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            torch.__Warnings.warn("Hardware support for type float16 is not available in the .NET Framework, so a software implementation is used. You should use float32 or float64 to speed up your application.");
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert Half to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">System.Half 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(Half[,,] data, torch.dtype dtype = torch.dtype.float16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            torch.__Warnings.warn("Hardware support for type float16 is not available in the .NET Framework, so a software implementation is used. You should use float32 or float64 to speed up your application.");
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert Half to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">System.Half 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(Half[,,,] data, torch.dtype dtype = torch.dtype.float16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            torch.__Warnings.warn("Hardware support for type float16 is not available in the .NET Framework, so a software implementation is used. You should use float32 or float64 to speed up your application.");
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert Half to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">System.Half 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(Half[,,,,] data, torch.dtype dtype = torch.dtype.float16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            torch.__Warnings.warn("Hardware support for type float16 is not available in the .NET Framework, so a software implementation is used. You should use float32 or float64 to speed up your application.");
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert Half to bool.");
                }
            }
        }

        //**************************************************
        //-> float array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">float 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(float[] data, torch.dtype dtype = torch.dtype.float32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert float to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">float 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(float[,] data, torch.dtype dtype = torch.dtype.float32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert float to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">float 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(float[,,] data, torch.dtype dtype = torch.dtype.float32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert float to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">float 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(float[,,,] data, torch.dtype dtype = torch.dtype.float32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert float to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">float 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(float[,,,,] data, torch.dtype dtype = torch.dtype.float32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert float to bool.");
                }
            }
        }

        //**************************************************
        //-> double array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">double 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(double[] data, torch.dtype dtype = torch.dtype.float64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert double to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">double 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(double[,] data, torch.dtype dtype = torch.dtype.float64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert double to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">double 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(double[,,] data, torch.dtype dtype = torch.dtype.float64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert double to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">double 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(double[,,,] data, torch.dtype dtype = torch.dtype.float64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert double to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">double 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(double[,,,,] data, torch.dtype dtype = torch.dtype.float64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert double to bool.");
                }
            }
        }

        //**************************************************
        //-> byte array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">byte 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(byte[] data, torch.dtype dtype = torch.dtype.uint8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert uint8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">byte 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(byte[,] data, torch.dtype dtype = torch.dtype.uint8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert uint8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">byte 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(byte[,,] data, torch.dtype dtype = torch.dtype.uint8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert uint8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">byte 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(byte[,,,] data, torch.dtype dtype = torch.dtype.uint8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert uint8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">byte 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(byte[,,,,] data, torch.dtype dtype = torch.dtype.uint8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert uint8 to bool.");
                }
            }
        }

        //**************************************************
        //-> sbyte array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">sbyte 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(sbyte[] data, torch.dtype dtype = torch.dtype.int8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">sbyte 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(sbyte[,] data, torch.dtype dtype = torch.dtype.int8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">sbyte 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(sbyte[,,] data, torch.dtype dtype = torch.dtype.int8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">sbyte 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(sbyte[,,,] data, torch.dtype dtype = torch.dtype.int8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int8 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">sbyte 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(sbyte[,,,,] data, torch.dtype dtype = torch.dtype.int8, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int8 to bool.");
                }
            }
        }

        //**************************************************
        //-> short array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">short 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(short[] data, torch.dtype dtype = torch.dtype.int16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int16 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">short 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(short[,] data, torch.dtype dtype = torch.dtype.int16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int16 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">short 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(short[,,] data, torch.dtype dtype = torch.dtype.int16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int16 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">short 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(short[,,,] data, torch.dtype dtype = torch.dtype.int16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int16 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">short 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(short[,,,,] data, torch.dtype dtype = torch.dtype.int16, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int16 to bool.");
                }
            }
        }

        //**************************************************
        //-> int array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">int 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(int[] data, torch.dtype dtype = torch.dtype.int32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int32 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">int 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(int[,] data, torch.dtype dtype = torch.dtype.int32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int32 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">int 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(int[,,] data, torch.dtype dtype = torch.dtype.int32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int32 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">int 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(int[,,,] data, torch.dtype dtype = torch.dtype.int32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int32 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">int 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(int[,,,,] data, torch.dtype dtype = torch.dtype.int32, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int32 to bool.");
                }
            }
        }

        //**************************************************
        //-> long array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">long 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(long[] data, torch.dtype dtype = torch.dtype.int64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int64 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">long 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(long[,] data, torch.dtype dtype = torch.dtype.int64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int64 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">long 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(long[,,] data, torch.dtype dtype = torch.dtype.int64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int64 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">long 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(long[,,,] data, torch.dtype dtype = torch.dtype.int64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int64 to bool.");
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">long 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(long[,,,,] data, torch.dtype dtype = torch.dtype.int64, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    __match(data, this.__data_float16);
                    break;
                }
                case torch.dtype.float32:
                {
                    __match(data, this.__data_float32);
                    break;
                }
                case torch.dtype.float64:
                {
                    __match(data, this.__data_float64);
                    break;
                }
                case torch.dtype.int8:
                {
                    __match(data, this.__data_int8);
                    break;
                }
                case torch.dtype.uint8:
                {
                    __match(data, this.__data_uint8);
                    break;
                }
                case torch.dtype.int16:
                {
                    __match(data, this.__data_int16);
                    break;
                }
                case torch.dtype.int32:
                {
                    __match(data, this.__data_int32);
                    break;
                }
                case torch.dtype.int64:
                {
                    __match(data, this.__data_int64);
                    break;
                }
                case torch.dtype.@bool:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert int64 to bool.");
                }
            }
        }

        //**************************************************
        //-> bool array to torch.Tensor
        //**************************************************

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">bool 1-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(bool[] data, torch.dtype dtype = torch.dtype.@bool, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 1;
            this.__width = data.Length;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float16.");
                }
                case torch.dtype.float32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float32.");
                }
                case torch.dtype.float64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float64.");
                }
                case torch.dtype.int8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int8.");
                }
                case torch.dtype.uint8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to uint8.");
                }
                case torch.dtype.int16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int16.");
                }
                case torch.dtype.int32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int32.");
                }
                case torch.dtype.int64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int64.");
                }
                case torch.dtype.@bool:
                {
                    __match(data, this.__data_bool);
                    break;
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">bool 2-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(bool[,] data, torch.dtype dtype = torch.dtype.@bool, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 2;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float16.");
                }
                case torch.dtype.float32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float32.");
                }
                case torch.dtype.float64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float64.");
                }
                case torch.dtype.int8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int8.");
                }
                case torch.dtype.uint8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to uint8.");
                }
                case torch.dtype.int16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int16.");
                }
                case torch.dtype.int32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int32.");
                }
                case torch.dtype.int64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int64.");
                }
                case torch.dtype.@bool:
                {
                    __match(data, this.__data_bool);
                    break;
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">bool 3-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(bool[,,] data, torch.dtype dtype = torch.dtype.@bool, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 3;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float16.");
                }
                case torch.dtype.float32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float32.");
                }
                case torch.dtype.float64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float64.");
                }
                case torch.dtype.int8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int8.");
                }
                case torch.dtype.uint8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to uint8.");
                }
                case torch.dtype.int16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int16.");
                }
                case torch.dtype.int32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int32.");
                }
                case torch.dtype.int64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int64.");
                }
                case torch.dtype.@bool:
                {
                    __match(data, this.__data_bool);
                    break;
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">bool 4-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(bool[,,,] data, torch.dtype dtype = torch.dtype.@bool, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 4;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float16.");
                }
                case torch.dtype.float32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float32.");
                }
                case torch.dtype.float64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float64.");
                }
                case torch.dtype.int8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int8.");
                }
                case torch.dtype.uint8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to uint8.");
                }
                case torch.dtype.int16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int16.");
                }
                case torch.dtype.int32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int32.");
                }
                case torch.dtype.int64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int64.");
                }
                case torch.dtype.@bool:
                {
                    __match(data, this.__data_bool);
                    break;
                }
            }
        }

        ///<summary>Initialize Tensor from a given standard .NET array.</summary>
        ///<param name="data">bool 5-dim array.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name="requires_grad">If false, torch.autograd will not record operations with this Tensor.</param>
        public Tensor(bool[,,,,] data, torch.dtype dtype = torch.dtype.@bool, bool requires_grad = false)
        {
            if(data == null)
            {
                throw new NullReferenceException("The parameter had an invalid null value.");
            }
            this.__ndim = 5;
            this.__width = data.GetLength(0);
            this.__height = data.GetLength(1);
            this.__depth = data.GetLength(2);
            this.__time = data.GetLength(3);
            this.__batch = data.GetLength(4);
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
            switch(this.dtype)
            {
                case torch.dtype.float16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float16.");
                }
                case torch.dtype.float32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float32.");
                }
                case torch.dtype.float64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to float64.");
                }
                case torch.dtype.int8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int8.");
                }
                case torch.dtype.uint8:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to uint8.");
                }
                case torch.dtype.int16:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int16.");
                }
                case torch.dtype.int32:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int32.");
                }
                case torch.dtype.int64:
                {
                    throw new torch.TorchException("TorchException: It is impossible to convert bool to int64.");
                }
                case torch.dtype.@bool:
                {
                    __match(data, this.__data_bool);
                    break;
                }
            }
        }

        //--------------------------------------------------
        //-> Initialization of tensor elements by zeros by tensor size and data type.
        //--------------------------------------------------

        ///<summary>Initializes a 0-dimensional tensor of the default data type and assigns all its elements a value of 0.</summary>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name = "requires_grad">If autograd should record operations on the returned tensor. Default: false.</param>
        public Tensor(torch.dtype dtype = torch.dtype.@default, bool requires_grad = false)
        {
            //-> Initialize the dimensions...
            this.__ndim = 0;
            this.__width = 1;
            this.__height = 1;
            this.__depth = 1;
            this.__time = 1;
            this.__batch = 1;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
        }

        ///<summary>Initializes a tensor of the default data type with the specified dimensions and sets all elements to 0.</summary>
        ///<param name = "width">The width of the tensor or 1.</param>
        ///<param name = "height">The height of the tensor or 1.</param>
        ///<param name = "depth">The depth of the tensor or 1.</param>
        ///<param name = "time">The number of times in the tensor (video) or 1.</param>
        ///<param name = "batch">The batch dimension. Highest dimension or 1.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name = "requires_grad">If autograd should record operations on the returned tensor. Default: false.</param>
        public Tensor(int width = 1, int height = 1, int depth = 1, int time = 1, int batch = 1, bool requires_grad = false, torch.dtype dtype = torch.dtype.@default)
        {
            //-> Initialize the dimensions...
            this.__RaiseIfDimensionIsInvalid(width, height, depth, time, batch);
            this.__ndim = 0;
            if(width > 1)
            {
                this.__ndim = 1;
            }
            if(height > 1)
            {
                this.__ndim = 2;
            }
            if(depth > 1)
            {
                this.__ndim = 3;
            }
            if(time > 1)
            {
                this.__ndim = 4;
            }
            if(batch > 1)
            {
                this.__ndim = 5;
            }
            this.__width = width;
            this.__height = height;
            this.__depth = depth;
            this.__time = time;
            this.__batch = batch;
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
            this.__InitializeData();
        }

        ///<summary>Initializes a tensor of the default data type with the specified dimensions and sets all elements to 0.</summary>
        ///<param name = "width">The width of the tensor or 1.</param>
        ///<param name = "height">The height of the tensor or 1.</param>
        ///<param name = "depth">The depth of the tensor or 1.</param>
        ///<param name = "time">The number of times in the tensor (video) or 1.</param>
        ///<param name = "batch">The batch dimension. Highest dimension or 1.</param>
        ///<param name = "dtype">Tensor data type.</param>
        ///<param name = "requires_grad">If autograd should record operations on the returned tensor. Default: false.</param>
        public Tensor(torch.Size size, torch.dtype dtype = torch.dtype.@default, bool requires_grad = false)
        {
            this.__ndim = size.__ndim;
            if(this.__ndim == 0)
            {
                this.__width = 1;
                this.__height = 1;
                this.__depth = 1;
                this.__time = 1;
                this.__batch = 1;
            }
            else
            {
                this.__width = size.__w;
                this.__height = size.__h;
                this.__depth = size.__d;
                this.__time = size.__t;
                this.__batch = size.__b;
            }
            //-> Initialize the data storage...
            if(dtype == torch.dtype.@default)
            {
                dtype = torch.default_dtype;
            }
            this.dtype = dtype;
            this.requires_grad = requires_grad;
            if((requires_grad) && (dtype != torch.dtype.float16) && (dtype != torch.dtype.float32) && (dtype != torch.dtype.float64))
            {
                throw new torch.TorchException("TorchException: Only Tensors of floating point dtype can require gradients.");
            }
            this.__InitializeData();
            if(requires_grad)
            {
                this.__InitializeGrad();
            }
        }

        ///<summary>Initializes a tensor with references to specified gradient stores as a data store. Required for the Tensor.grad property.</summary>
        ///<param name = "grad_float16">float16 gradient.</param>
        ///<param name = "grad_float32">float32 gradient.</param>
        ///<param name = "grad_float64">float64 gradient.</param>
        ///<param name = "ndim">Number of dimensions of the tensor.</param>
        ///<param name = "width">The width of the dectination tensor.</param>
        ///<param name = "height">The height of the dectination tensor.</param>
        ///<param name = "depth">The depth of the dectination tensor.</param>
        ///<param name = "time">The time of the dectination tensor.</param>
        ///<param name = "batch">The batch of the dectination tensor.</param>
        private Tensor(Half[] grad_float16 = null, float[] grad_float32 = null, double[] grad_float64 = null, byte ndim = 0, int width = 1, int height = 1, int depth = 1, int time = 1, int batch = 1)
        {
            if(grad_float16 != null)
            {
                if((grad_float32 != null) || (grad_float64 != null))
                {
                    throw new torch.TorchException("Internal torch.TorchException: tensor must have single - typed gradient.");
                }
                this.__ndim = ndim;
                this.__width = width;
                this.__height = height;
                this.__depth = depth;
                this.__time = time;
                this.__batch = batch;
                this.dtype = torch.dtype.float16;
                this.__data_float16 = grad_float16;
            }
            else
            {
                if(grad_float32 != null)
                {
                    if((grad_float16 != null) || (grad_float64 != null))
                    {
                        throw new torch.TorchException("Internal torch.TorchException: tensor must have single - typed gradient.");
                    }
                    this.__ndim = ndim;
                    this.__width = width;
                    this.__height = height;
                    this.__depth = depth;
                    this.__time = time;
                    this.__batch = batch;
                    this.dtype = torch.dtype.float32;
                    this.__data_float32 = grad_float32;
                }
                else
                {
                    if(grad_float64 != null)
                    {
                        if((grad_float16 != null) || (grad_float32 != null))
                        {
                            throw new torch.TorchException("Internal torch.TorchException: tensor must have single - typed gradient.");
                        }
                        this.__ndim = ndim;
                        this.__width = width;
                        this.__height = height;
                        this.__depth = depth;
                        this.__time = time;
                        this.__batch = batch;
                        this.dtype = torch.dtype.float64;
                        this.__data_float64 = grad_float64;
                    }
                    else
                    {
                        throw new torch.TorchException("Internal torch.TorchException: tensor must have single - typed gradient.");
                    }
                }
            }
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Half.</param>
        public static implicit operator Tensor(Half data)
        {
            var r = new Tensor(torch.dtype.half, true);
            r.__data_float16[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Single (C# float).</param>
        public static implicit operator Tensor(float data)
        {
            var r = new Tensor(torch.dtype.@float, true);
            r.__data_float32[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Double (C# double).</param>
        public static implicit operator Tensor(double data)
        {
            var r = new Tensor(torch.dtype.@double, true);
            r.__data_float64[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.SByte (C# sbyte).</param>
        public static implicit operator Tensor(sbyte data)
        {
            var r = new Tensor(torch.dtype.int8);
            r.__data_int8[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Byte (C# byte).</param>
        public static implicit operator Tensor(byte data)
        {
            var r = new Tensor(torch.dtype.uint8);
            r.__data_uint8[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Int16 (C# short).</param>
        public static implicit operator Tensor(short data)
        {
            var r = new Tensor(torch.dtype.@short);
            r.__data_int16[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Int32 (C# int).</param>
        public static implicit operator Tensor(int data)
        {
            var r = new Tensor(torch.dtype.@int);
            r.__data_int32[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard number to 0-dim Tensor.</summary>
        ///<param name="data">System.Int64 (C# long).</param>
        public static implicit operator Tensor(long data)
        {
            var r = new Tensor(torch.dtype.@long);
            r.__data_int64[0] = data;
            return r;
        }

        ///<summary>Converts the .NET standard bool to 0-dim Tensor.</summary>
        ///<param name="data">System.Boolean (C# bool).</param>
        public static implicit operator Tensor(bool data)
        {
            var r = new Tensor(torch.dtype.@bool);
            r.__data_bool[0] = data;
            return r;
        }

    }

}